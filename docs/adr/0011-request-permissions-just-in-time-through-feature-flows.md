# Request Permissions Just in Time Through Feature Flows

Desktop Island should not request Calendar, Camera, Automation, or similar permissions on first launch. Permissions are requested only when the user enables or opens the feature that needs them, and Permission Center explains current status, usage, and revocation.

This keeps onboarding light and protects trust. A user installing a top-center productivity utility should see the Island quickly, not a wall of system permission prompts.
